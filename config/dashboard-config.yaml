# OdhDashboardConfig — default dashboard configuration for RHOAI.
# Enables all features for parity. On a laptop, some features
# (GPU metrics, NIM serving) may have no backing infrastructure.
#
# Source: https://ai-on-openshift.io/odh-rhoai/gitops
apiVersion: opendatahub.io/v1alpha
kind: OdhDashboardConfig
metadata:
  name: odh-dashboard-config
  namespace: redhat-ods-applications
  labels:
    app.kubernetes.io/part-of: rhods-dashboard
    app.opendatahub.io/rhods-dashboard: "true"
spec:
  dashboardConfig:
    enablement: true
    disableAcceleratorProfiles: false
    disableBYONImageStream: false
    disableClusterManager: false
    disableCustomServingRuntimes: false
    disableDistributedWorkloads: false
    disableHome: false
    disableISVBadges: false
    disableInfo: false
    disableKServe: false
    disableKServeAuth: false
    disableKServeMetrics: false
    disableModelMesh: false
    disableModelRegistry: false
    disableModelServing: false
    disablePerformanceMetrics: false
    disablePipelines: false
    disableProjectSharing: false
    disableProjects: false
    disableStorageClasses: false
    disableSupport: false
    disableTracking: false
    disableTrustyBiasMetrics: false
  groupsConfig:
    adminGroups: rhods-admins
    allowedGroups: "system:authenticated"
